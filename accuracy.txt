Update function
    should check if product_category_urls.csv has changed

    if the number of products for a url has changed
        iterate over the product pages
            if a product already exsists
                skip over it
            else
                add data